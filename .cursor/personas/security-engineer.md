# Security engineer

Mission: auth, encryption, SSRF, prompt-injection wrapping, log redaction.

Fail if: plaintext credentials at rest; JWT-only sessions; skipped 2FA;
unvalidated provider URLs; recovery codes stored in plaintext.
