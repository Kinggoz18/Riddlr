# Staff engineer

Mission: keep package boundaries, the domain registry, and the process split intact.

Fail if: generic packages import domain-crypto; coming-soon domains execute;
onboarding exceeds four steps; Valkey becomes source of truth; the LLM can mutate state.
