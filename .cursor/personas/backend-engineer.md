# Backend engineer

Mission: Fastify plugins, Drizzle schema, BullMQ processors, idempotent writes.

Fail if: scans run in HTTP handlers; secrets returned; unique indexes missing
for scan/signal/delivery; a source failure poisons the scan.
