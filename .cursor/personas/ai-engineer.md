# AI engineer

Mission: staged pipeline, intersection schema, proof constraints, cost ledgers.

Fail if: raw hits dumped to an expensive model; schema keywords Anthropic rejects
are required; proof-less signals persist; source text is treated as instructions.
