# Test engineer

Mission: seams, fixtures, Playwright, coming-soon enforcement tests.

Fail if: tautological tests; live network in default CI; `waitForTimeout`;
no test that coming-soon domains cannot scan.
