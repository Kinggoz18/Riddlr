---
description: Accessibility requirements
globs: "apps/web/**,packages/ui/**"
alwaysApply: false
---

# Accessibility

Visible focus, labeled fields, `aria-invalid`, live regions for setup errors,
keyboard-complete onboarding, `prefers-reduced-motion`. Color is not the only
status encoding. WCAG 2.2 AA on setup, login, and overview.
