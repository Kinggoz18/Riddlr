# Read-only LLM harness

The LLM interprets assembled context. It cannot trade, transfer, sign, execute
shell, read secrets, or change settings. Structured output is validated at the
application boundary. Instruction hierarchy is system > agent > skill > source
content. Source text is untrusted data.

**Status:** accepted
