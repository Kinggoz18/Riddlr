# WhatsApp Cloud API

Official Graph messages endpoint with HMAC-SHA256 webhook signatures
(`X-Hub-Signature-256`). Session text is limited to the 24-hour
customer-service window after authentic inbound webhook traffic. Default Graph
version is v22. Unofficial WhatsApp clients are not used. Secrets are encrypted
at rest.

**Status:** accepted
